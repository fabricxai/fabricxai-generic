# STUBS — temporary cross-module fakes (replace within a week of the real module landing)

> **The rows below are not stubs in the original sense** — nothing was faked and no
> module is waiting on another. They are load-bearing gaps found by the 2026-08-05
> re-verification, recorded here because this file is where this project keeps the things it
> knows are not true yet. Each is a task in
> [`docs/PRODUCTION-READINESS-PLAN.md`](./PRODUCTION-READINESS-PLAN.md).

| Stub | Faked contract | Owner module | Created | Replace by |
|---|---|---|---|---|
| Fifteen modules still have no HANDOFF | Plan 8.1 backfilled the eight pilot modules — store, cutting, production, quality, sampling, commercial, workforce, approvals — and `handoff-contract.test.ts` keeps their §5/§6/§7 matching the code. The other fifteen have only their brief. That is a gap in acceptance checklists rather than in the build, and it is deliberate: the pilot set is what a factory touches first. `CLAUDE.md` now states the rule honestly — it has never been honoured, and it stands for what is built next rather than describing what happened. | all | 2026-08-07 (plan 8.1) | As each remaining module reaches a pilot |
| ~~A cost-sheet consumption under 0.01 costs nothing~~ **resolved, and it was worse than the row said** | The FOB breakdown read every input at scale 2. Two columns are not: `bom_lines.consumption` is `numeric(12, 4)` and `cost_sheets.fx_rate_local_to_base` is `numeric(12, 6)`. So a trims line of 0.0083 kg per piece cost the quote nothing — and **BDT→USD ≈ 0.0083 truncated to 0.00, making the CM component ZERO on every sheet priced in local currency**, which is every sheet. CM is usually the largest single part of a garment's FOB. Nothing looked broken because the sheet's stored total is computed by costing through `lib/money.convert` and stayed correct, while `commercial` is derived as total-minus-named — so the entire CM cost silently landed there and the reconciliation invariant still passed. Fixed by reading each column at its own declared scale through a new shared `toMinorAtScale`; red-tested (the new case fails with "expected 0 to be greater than 0" against the old arithmetic). | rfq, finance | 2026-08-07 (plan 2.9) | ✅ `toMinorAtScale`, and an integration case that pins CM ≠ 0 |
| ~~Nothing stops somebody approving their own draft~~ **resolved** | Two halves, landed a session apart. The BAN came first: `approve()` refuses `draft.createdBy === ctx.userId` on any table a module registered with `registerAuditedTables`, and leaves the intake flow alone everywhere else — so a merchandiser still reviews and signs their own enquiry extraction, and nobody signs their own credit. The DECISION this row was actually waiting on is now made too, and it is the per-rule flag: `approval_rules.self_approval_allowed`, nullable so that a rule written for some other reason keeps saying nothing about it, with the module's `approvalDefaults.selfApprovalAllowed` behind it and a toggle in Settings → Approval routing in front. **The carve-out is narrower than the flag**: even switched on, core refuses anything but `ai_extraction` — a draft somebody typed, a chat-composed one, an import — because the second party the ⚖ mark promises is the extractor that read the paper, and a typed draft has no second party at all. Eight integration cases hold each edge. | core → approvals | 2026-08-06 (found in plan 3.1) | ✅ 2026-08-29 |
| Six approvals functions had no caller anywhere in the product | `inboxCounts`, `approversFor`, `auditChain` and `correctionRates` were exported, tested by nothing, and reached by no page, action, job or tool; `agingDrafts` was reachable only through the nightly escalation job. Plan 3.1 gives those five their first caller as MARBIM read tools, and 3.2 their first tests. The sixth is a WRITE: `upsertApprovalRule` gained its door in adoption plan 3.2 — Settings → Approval routing, owner-only, with `deactivateApprovalRule` beside it. What remains without a screen is the audit chain ("who let this row in", asked months later when a figure is disputed): a query with no UI, and the redacted MARBIM answer is not a substitute for one. | approvals | 2026-08-06 (found in plan 3.1/3.2) | audit-chain UI with plan 5.5 |
| ~~Tablet layout is verified by static analysis, not by eye~~ **partly resolved** | Plan 4.4 fixed the structural half — the flex item that could not shrink, the sidebar that took a third of a 768px screen, the four seven-column tables that squeezed every column to 85px — and `tablet-layout.test.ts` keeps a new screen from arriving the same way. What it cannot see is TEXT: a 1fr column can be legible at 200px and useless at 95px, and no source scan tells the difference. Plan 7.2 installed a browser and a `tablet` Playwright project at 768×1024, and it immediately found what static analysis could not: every `.fx-scroll-x` wrapper unfocusable by keyboard, and two design tokens below WCAG AA on backgrounds only the tablet layout uses. All fixed. **What a machine still cannot judge** is whether a 1fr column at 200px is legible in Bangla at arm's length under a factory light — axe checks contrast and names, not whether a person can read it standing up. Somebody still has to open the thirteen floor routes and look. | shell → all floor screens | 2026-08-06 (plan 4.4 → 7.2) | Before pilot — a person with a tablet |
| The two remaining offline bypasses are by design, and now say so | Rule 7 sends floor-facing writes through the batch endpoint. Four screens posted straight to a server action; plan 4.1 moved the two that had no reason (`quality/final`, `quality/measurements`). These two stay, with the rationale in their action docstrings: **`quality/fabric`** grades cloth at a fixed inspection frame in the store, mains-powered, metres from the router — queuing buys nothing and lets two inspectors grade the same roll on two devices with no way to reconcile which sheet is real. **`store/rolls`** drafts a stock *adjustment*, which is not a record of a physical event but somebody asserting the shelf disagrees with the system; it goes through `pending_changes` and nothing is written until a reviewer signs it, so there is nothing to replay. | quality, store | 2026-08-06 (plan 4.1) | — by design |
| 13 server actions have no screen behind them | Down from 17. `action-reachability.test.ts` is the ratchet: it scans every `modules/*/actions.ts` export, fails any nothing references, and holds them in a shrink-only `NO_SCREEN_YET` list with the reason for each — so the gap is a build property rather than a line in an audit. Remaining: compliance can record no audit, training, CAP or certificate; workforce can enter no wage gazette and activate none; procurement can enter no quote by hand and move no PO through its lifecycle; a shipment cannot be opened; a sample cannot be requested (the PP gate depends on one); a machine cannot be reported broken; the similar-style lookup has no entry point; and `orders/generateOrderTna` awaits a template picker. Two more — `quality/recordMeasuredPieces` and `submitFinalInspection` — were orphaned deliberately when 4.1 moved their screens to the offline queue: delete or wire, but decide. | all | 2026-08-06 (plan 5.5) | Plan 5.5, one module per session |
| A registered zod key can be un-committable: `planning/allocations` + `allocation` | `resolvePendingSchema` validates a draft against `zodMap[zodSchemaKey]` and never checks that any commit path accepts that key. Planning registers both `allocation` and `scenario_apply`; the `allocations` commit handler parses **only** `applyScenarioPayload`. So a draft carrying one allocation validates at propose, renders in the inbox, reads perfectly to a reviewer — and throws at the click, which is the exact failure TEST-H4 exists for. No producer today (planning ships no draft tool), so it is a trap rather than a live bug. Contrast `finance/payables`, which does this correctly: `commitPayable` branches on operation, so `payable` and `pay_payable` both commit — and both are now covered. | planning → core | 2026-08-06 (found in plan 3.3) | When planning gains a draft tool, or with a check that every zodMap key has a commit path |
| The aging escalation is not idempotent, and its comment said it was | `emitAgingEscalations` was documented "idempotent per run by the outbox's own dedupe". `emit` is a plain INSERT with no dedupe key; the dedupe that exists is consumer-side (`markProcessed`) keyed on the outbox row's own id, which is new on every insert. The scheduler runs this daily and a draft stays aging until somebody acts, so a draft ignored for a week raises seven escalations. Arguably right for something that is by definition being ignored — the objection is that it was never decided, only assumed away. The comment is corrected and an integration case pins the current behaviour so a change to it is visible. | approvals → core (outbox) | 2026-08-06 (found in plan 3.2) | When escalation levels are designed — plan 3.x or the notifications screen |
| ~~Server actions have no role authorization~~ **resolved** | `requireRole` had zero callers; all 16 `modules/*/actions.ts` were session-only, so any member could open the PP gate, issue a PO, open a BTB, confirm ex-factory or open an LC submission. **69 call sites across 16 modules now gate on roles**, derived from the sync handlers, `nav.ts` audiences and the services that already gate themselves; no action is broader than its module's nav entry. A source sweep fails any ungated export and 6 integration cases prove it against real sessions. Payroll's stricter hr+owner rule stays in 10.1's service. | core → all 16 action files | 2026-08-05 (N1) | ✅ `5b92922` (plan 1.1) |
| A hardened (non-superuser) owner needs three superuser grants nobody documented | Found by the `owner-privileges` CI job (plan 1.5). `BYPASSRLS` is necessary and not sufficient: `vector` is an untrusted extension, the owner needs `CREATEROLE`, and **`app.pgbouncer_get_auth` is SECURITY DEFINER over superuser-only `pg_shadow`** — without `GRANT SELECT ON pg_shadow` the pooler's auth_query fails and PgBouncer refuses every client. All three are now in `deploy.md`, and `db:setup-roles` warns about the third. Not fixable in code: granting on a system catalogue is a superuser act. | core → ops | 2026-08-06 (DB-B1) | ✅ documented + CI-gated `4a36033` |
| ~~Wall 1 (the `company_id` predicate) covers 1 module of 23~~ **resolved** | CLAUDE.md rule 2 says RLS is "the second wall, never the only wall"; it was the only wall across 466 query sites (audit BE-B1). Now every module and every query file names its company — 49 files on the `require-tenant-predicate` ratchet, converted module by module in risk order. Two tables are permanently exempt and the type system enforces it: `aql_tables` and `users` have no `company_id` and `scoped()` refuses them. `two-walls.integration.test.ts` proves the walls fail independently by disabling RLS and showing the unscoped query read the other company's row while the scoped one did not. | core → all | 2026-08-06 → 2026-08-07 (BE-B1) | ✅ plan 1.3 |
| ~~Five auth tables have no RLS at all~~ **resolved** | Migration `0073` gives `users`, `profiles`, `sessions`, `accounts` and `verifications` scope-conditional policies: unscoped behaves as before (Better Auth queries these outside `withTenantTx`, so deny-all would break every login), while under a tenant scope `users`/`profiles` narrow to people sharing the company and the three secret-bearing tables return nothing. Measured on the app role: 8 users visible scoped, 26 without the policy. **Known limit, in the migration header:** this walls off module code, not the pooled handle — closing that means giving auth its own DB role. | core | 2026-08-05 (N2) | ✅ `329e52d` (plan 1.2) |
| ~~`MARBIM_ENABLED` is a boot assertion, not a switch~~ **resolved** | Consumed in six places (plan 6.1): the shell hides the FAB, the panel and the nav entries; `/marbim`, `/marbim/intake` and `/memory` return `LockedState`; `readDocument` refuses on the flag **and** on `hasProvider()`, at the door, so nothing queues into a void; `activeScheduledTasks()` is the one list the registration and both health checks read. `job_run_status` gained `skipped`, so the poller's decline no longer closes as a success and job health stops reporting green over an unread pile. "Pilot with MARBIM off" is now a supported configuration, guarded by `marbim-off.test.ts`. | marbim → core | 2026-08-05 → 2026-08-06 | ✅ (plan 6.1) |
| ~~Global search reads six modules' tables directly~~ **resolved** | The reads moved to each owner's `queries.ts` (rule 11), each scoped with the wall-1 helper; the shell keeps only the access decision and the wording. The lint glob that missed it — `src/components/**` and `src/modules/*/actions.ts` — now covers both and bans schema/drizzle imports there. 3-char minimum, a 60/min bucket and `server-only` closed the rest. | shell → owners | 2026-08-05 | ✅ `e922844` (plan 1.4) |
| The final-inspection waiver is unreachable from any screen | `waiveFinalInspection` blocks a departure and has **no `actions.ts` export and no UI caller** — so a shipment held by a failed QC inspection cannot be released from the product at all, only from a database console. Found while wiring the LC date waiver, which now has both. Same class as the 16 orphaned actions in plan 5.5, but sharper: this one is the escape hatch for a gate that already blocks today. | shipment (8.1) | 2026-08-06 | Plan 5.5 — or sooner, it is one action and one panel |
| Seed writes document *rows*, not objects | No bytes go to MinIO, so seeded documents cannot be downloaded. Status is `uploaded`, never `ready`, so the data does not lie about it. | core | 2026-07-29 | When a demo needs real files |
| ~~`modules/core/gates.ts` — shapes only~~ **resolved** | All seven gates are implemented and called: PP-approval (1.4→5.1), fabric inspection (7.1→3.1), UD balance (2.2, exact BigInt inside the issue transaction), BTB headroom (2.1 + 3.2), EXP number (8.1), final inspection (8.1, with a role-gated waiver). Both seam gates fail CLOSED with no provider registered. | core → owners | 2026-07-29 | ✅ all seven (`lcLatestShipment` closed 2026-08-06) |
| ~~`GATES.lcLatestShipment` is declared and never enforced~~ **resolved** | The seventh gate now blocks: `confirmExFactory` refuses a departure the credit cannot accept by date (`latest_shipment` or `expiry`), and `waiveLcDate` lets commercial or the owner accept the breach on the record first — reason required, audited, refused once the departure is recorded. `presentation_window` remains advisory on purpose: the shipping date is fine there and the risk is document turnaround. All seven gates in CLAUDE.md rule 8 are now enforced. | commercial (2.1) | 2026-08-03 (audit BE-H2) | ✅ `05f9b7e` (plan 2.1) |
| ~~Screen copy is Bangla on 1 of 12 floor routes~~ **resolved for the floor 2026-08-03** | All twelve floor routes render Bangla through `lib/i18n-ui.ts` + `requestLocale` + `useT` (16 floor pages call `requestLocale`), the language picker ships in the account menu, and the floor error namespaces are at full parity. | core → all screens | 2026-08-03 (audit FE-B1) | ✅ for the floor — residue in the row below |
| ~~The chrome around the Bangla screens is still English~~ **resolved** | All 24 nav labels, 5 section headings and 17 role names come from `ui.nav.*` / `ui.role.*`, keyed on the NAV entry's id, and `nav-copy.test.ts` walks the registry to demand both locales — a route added without its copy fails there rather than showing an English word in a Bangla sidebar. The four `(auth)` pages, `ReadOnlyNote`, `LockedState`, the search placeholder and MARBIM's scope chip are converted too, and `shipment.errors` is at full parity. **Owed:** 158 refusal keys are still English-only (was 184), which plan 4.3 pins so the number can only shrink. | core → shell, i18n | 2026-08-05 → 2026-08-06 | ✅ (plan 4.2); the refusal residue is 4.3 |
| `renderPdf` queue has no worker, so nothing routes to it | `procurement.po.issued` and `shipment.packing_list.approved` were routed there and parked forever; the routes are removed and those events now fall through to `notify`. The worker asserts at boot that every routed queue has a worker, so restoring the routes without the render worker fails the boot rather than leaking jobs. | worker | 2026-08-03 (audit INFRA-B6) | With the Playwright PDF pipeline |
| PgBouncer **dev** auth is `auth_type = plain` with a plaintext `userlist.txt` | Fine for a local compose file whose passwords are already in `.env.example`. **Production is done:** `pgbouncer.prod.ini` uses `scram-sha-256` with `auth_user` + `auth_query` against a NOINHERIT lookup role (migration 0070), and prod compose mounts that file, not this one. The dev file stays plain and is clearly labelled. | core | 2026-07-29 | ✅ for production (audit INFRA-B4) |
| Company creation bypasses the organization plugin's endpoint | Signup creates the company + owner role in a `databaseHooks.user.create.after` hook writing `companies`/`roles` directly, with `allowUserToCreateOrganization: false`. Correct for one-factory-per-owner; multi-company onboarding and invitations are unexercised. | core | 2026-07-29 | X.3 Settings (invites, role matrix) |
| `session.create.before` picks the user's *first* membership as the active company | Fine while every user belongs to exactly one factory. A user in two companies gets an arbitrary one until a company switcher exists. | core | 2026-07-29 | X.3 Settings |
| `lib/{dates,pdf}.ts` — constants only | Timezone-explicit date helpers and the Playwright PDF pipeline. `money.ts`, `quantity.ts`, `ids.ts`, `s3.ts` and now `i18n.ts` + `i18n-ui.ts` ARE implemented. **`dates.ts` is the urgent one:** 85 sites compute "today" as `toISOString().slice(0,10)`, which is yesterday between 00:00 and 05:59 Dhaka — the night shift and every nightly cron. | core | 2026-07-29 | dates → before pilot (audit INFRA-H2) · pdf → Phase 6 |
| ~~No lint rules yet~~ **resolved** | `no-float-money` and `analytics-no-writes` exist in `eslint-rules/`, run as `error` in CI, and are themselves unit-tested with both valid and invalid cases. `no-float-money` was extended on 2026-08-03 to catch the `Number.parseFloat` spelling, which had been invisible to it at 22 call sites including the FOB price computation. Rules 4 and 9 are gates, not conventions. | core | 2026-07-29 | ✅ done |
| `buyers` table is minimal | Only `code`/`name`/`country` — enough for orders and LCs to hold a real FK. Module 1.1 owns it and extends additively. | buyers (1.1) | 2026-07-29 | When 1.1 is built |
| `btb_lcs.supplier_id` has no FK | `suppliers` belongs to module 3.2 (Procurement) and does not exist yet, so the column is an unconstrained uuid. | commercial | 2026-07-29 | Phase 6 (3.2) |
| 1.3 has no `actions.ts` | `queries.ts` landed with the screens; the write surface did not. The flagship module cannot be operated: `actualizeMilestone` is called only by the worker, and `MilestoneTimeline`'s `onActualize` prop has no caller — `orders/[orderId]/page.tsx` never passes it. A merchandiser cannot tick a milestone, record ex-factory or upload a buyer revision. Same shape in 1.2 rfq and 4.1 planning, which have no `actions.ts` at all. | orders | 2026-07-29 → 2026-08-05 | Plan 5.1 (audit FE-B2 / BE-B6) |
| `ex_factory_slipped` event carries `lcConflict: null` | The consumer runs the detector against live credits under its own scope. Embedding an answer at emit time would age badly in a queue. | orders | 2026-07-29 | With the event's consumer (Phase 3 digest) |
| TNA uses calendar days, not working days | A Bangladeshi factory calendar (two lunar Eids, national days, factory closures) needs a holidays table. Template offsets currently absorb weekends. | orders | 2026-07-29 | When a factory supplies its calendar |
| `orders` is a pending target with no commit handler | Drafting a whole new order from a PO scan still uses core's generic single-row write. Adequate today; revisit if creating an order grows side effects (TNA generation, LC linking). | orders | 2026-07-29 | When order creation gains side effects |
| ~~No `queries.ts` / `actions.ts` for commercial~~ **resolved** | Both landed; all 8 commercial actions have UI callers, the best per-module coverage in the repo. The residue is that **no LC and no UD can be created from any screen** — there is no `createLc`/`createUd` action, so both desks only ever operate on records something else made. | commercial | 2026-07-29 → 2026-08-05 | Plan 5.5 |
| UD reconciliation PDF not generated | `snapshotReconciliation` freezes the figures; rendering the customs-format PDF needs the Playwright pipeline. | commercial | 2026-07-29 | Phase 6 (first PDF consumer) |
| `runReconciliationReminder` is not scheduled | It is monthly, and SCHEDULED_TASKS currently only carries nightly crons. | commercial | 2026-07-29 | When a monthly cadence is added |
| `wage_gazettes` is a parent table the brief does not list | The brief puts `gazette_version` flat on `wage_grades`. Splitting the parent out is what makes upload-then-activate atomic — activating half a gazette would pay some grades at new rates and some at old ones in one run. | workforce | 2026-07-29 | Reconcile in HANDOFF-10.1 §4 |
| `workers.line_id` has no FK | `lines` belongs to module 4.1 (Planning) and does not exist yet. | workforce | 2026-07-29 | Phase 5 (4.1) |
| Disbursement export and payslip PDF not built | Bank/bKash sheet formats and the bn+en payslip need the Playwright pipeline and real format specs from the factory's bank. | workforce | 2026-07-29 | Phase 8 |
| Attendance device importer not built | Device CSV formats vary by vendor; the brief calls for pluggable parsers. Attendance is currently written directly. | workforce | 2026-07-29 | Phase 8 |
| **Payroll has NOT been parallel-run** | PLAYBOOK §3 requires one full month diffed against the factory's existing sheet before any real go-live. The engine is vector-tested; it has never met a real gazette or real attendance. Plan 7.4 built the tool — `pnpm payroll:parallel-run`, proven end to end against a computed run — so the gate is now executable rather than aspirational. **What is still owed is the run itself**, which needs a factory: a real gazette, a real month of attendance, and their own sheet. No amount of engineering closes this row; somebody has to do it with a customer. | workforce | 2026-07-29 → 2026-08-07 | Before first factory go-live — non-negotiable |
| `grns.supplier_po_id` has no FK | `supplier_pos` belongs to module 3.2 (Procurement). | store | 2026-07-29 | Phase 6 (3.2) |
| `ud_consumptions.store_issue_id` still has no FK | 3.1 now exists and populates it, but adding the constraint is a separate migration in commercial (rule 11: one writer module per table). | commercial | 2026-07-29 | Next commercial slice |
| Store returns and inspection flow not built | `rolls.status` has `returned` and `grns.inspection_status` exists, but no service operation moves them. | store | 2026-07-29 | With HANDOFF-3.1 |
| No k6 scenario for the store | 3.1 is floor-facing, so dev-plan §9 wants `store_grn.js`. | store | 2026-07-29 | Phase 4 close |
| `downtimes.machine_id` / `ticket_id` still have no FK | 9.1 now consumes the machine-downtime event and raises a ticket carrying `downtime_id`, so the link exists and is unique — but it points from 9.1's table to 6.1's, not back. Adding the reverse FK would make `downtimes` (6.1's table, rule 11) depend on a 9.1 table. The link is enforced by the unique index on `tickets.downtime_id` instead. | production → maintenance | 2026-07-29 | If the reverse lookup is ever needed |
| `wip_snapshots.cut` and `.finished` are always 0 | Cut belongs to 5.1 and finished to 8.1. Recorded as zero rather than guessed, so the gap is visible on the dashboard instead of being papered over with a plausible number. | production | 2026-07-29 | Phase 6 (5.1, 8.1) |
| **`k6/production_burst.js` has never been RUN** | Written first per PLAYBOOK §3 so the first run is a baseline rather than a tuned number. It needs a seeded factory, real line ids and an auth cookie, and it must run on VPS-class hardware — a laptop result would be meaningless. Phase 4 cannot close until it passes. | production | 2026-07-29 | Phase 4 close — non-negotiable |
| No HTTP routes for 6.1 yet | k6 posts to `/api/production/outputs` and `/api/production/board`; those are `actions.ts`/route work and await HANDOFF-6.1. The service functions behind them are built and tested. | production | 2026-07-29 | When HANDOFF-6.1 lands |
| `compareActual(orderId)` not built | The variance waterfall reads `order_costs_actual`, owned by module 11.1 Finance, which does not exist. | costing | 2026-07-29 | Phase 7 (11.1) |
| `refreshTemplate(fromOrderId)` not built | Refreshing a consumption template from a closed order's actuals needs 1.6 Order Memory outcomes. `touchTemplate` tracks usage only. | costing | 2026-07-29 | With 1.6 |
| FX rate is entered by hand per sheet | Snapshotting it is correct and deliberate; sourcing it is not solved. Somebody types today's rate, and nothing checks it against a reference. | costing | 2026-07-29 | Before pilot |
| Nightly plan-vs-actual variance job not built | The brief wants a nightly job reading 6.1 `efficiency_daily` to draft replan suggestions. The read side exists in 6.1; the job and its suggestion drafts do not. | planning | 2026-07-29 | Phase 5 close |
| `allocations` are not linked to 1.3 TNA milestones | `planning.sewing_window.changed` is emitted on every placement and date move, but nothing consumes it, so moving sewing does not yet ripple the order's milestones. | planning → orders | 2026-07-29 | When a queue consumer family exists (X.1) |
| Changeover-density warning is per line-day only | The brief also wants it as a standalone job over the whole board. `checkLineDayLoad` raises it for the day being checked; nothing sweeps the plan for it. | planning | 2026-07-29 | Phase 5 close |
| ~~No planning seed slice~~ **superseded** | Fourteen slices are registered including `PLANNING_SLICE`. The gap moved: **eight modules are still unseeded** — orders, costing, rfq, buyers, marbim, memory, analytics, settings, approvals — and `orders` is the load-bearing one, because slices downstream reference an order that only `pnpm demo` creates. `SeedSlice` has no `dependsOn` field either, so the order is a hand-maintained array whose correctness lives in comments and fails at runtime on an FK, not at review (audit DB-H5). | core → all | 2026-07-29 → 2026-08-05 | Phase 5 close |
| Cutting demoability now needs a REAL PP approval | The gate is live (1.4 registers the provider), so a fresh database still cannot cut until a PP sample is approved. Use `seedApprovedPpSample` from `modules/sampling/demo.ts`; `registerDemoPpApprovalBypass` is the escape hatch and refuses under NODE_ENV=production. | sampling | 2026-07-30 | When the seed slices land |
| Bundle QR ticket render not built | `qr_token` is generated and scannable; producing the printable ticket is UI/PDF work. | cutting | 2026-07-29 | With HANDOFF-5.1 |
| No k6 scenario for cutting | 5.1 is floor-facing, so dev-plan §9 wants `cutting_lay.js`. | cutting | 2026-07-29 | Phase 4 close |
| Supplier `qualityRejectPct` is always computed from zero rejects | The scorer takes reject quantities and the vectors cover them, but `computeSupplierScores` passes `'0'`: 3.1 records inspection status per GRN, not per PO line, so there is no reject QUANTITY to attribute to a supplier yet. Reporting a fabricated number would be worse than reporting none. | procurement → store | 2026-07-29 | When GRN inspections carry rejected qty |
| Supplier `priceIndex` is always null | Needs a basket average across suppliers for the same item over the period. The arithmetic exists and is vector-tested; the basket query does not. | procurement | 2026-07-29 | Phase 6 close |
| `responsivenessPct` counts returned quotes as requested quotes | There is no record of an RFQ having been SENT, so requested == returned and the metric is always 100%. Module 1.2 RFQ owns the sent side. | procurement → rfq | 2026-07-29 | Phase 6 (1.2) |
| PO PDF render + email not built | The brief wants "PO issue (PDF render on letterhead + email)". `document_id` is on the row and ready; producing the PDF and sending it is document/notification work. | procurement | 2026-07-29 | With HANDOFF-3.2 |
| GRN → PO line matching is not wired from the store side | `applyReceipt` exists and is tested, but 3.1's GRN flow does not call it — `grns.supplier_po_id` has no FK and nothing populates it. | store → procurement | 2026-07-29 | Next store slice |
| `btb_lcs.supplier_id` FK is enforced in SQL but not modelled in Drizzle | Migration 0030 adds it. Declaring it in `commercial/schema.ts` would make commercial and procurement schemas import each other, since procurement already imports `btb_lcs` for the gate. Drizzle diffs its own snapshot, so the unmodelled constraint is invisible to `db:generate` and safe. | commercial | 2026-07-29 | If drizzle gains a cycle-free FK declaration |
| Sample photos are rows only | `sample_photos` and `documents` exist; no upload path writes bytes to MinIO, same as the core seed's documents. | sampling | 2026-07-30 | When a demo needs real files |
| Sample due-date reminder job not wired | `overdueSamples()` is the query the reminder needs and is tested; there is no BullMQ processor calling it, because no consumer family exists yet. | sampling | 2026-07-30 | When a queue consumer family exists (X.1) |
| `sample_requests.rfq_id` has no FK | Module 1.2 RFQ does not exist. The xor constraint against `order_id` is enforced; the reference is not. | sampling → rfq | 2026-07-30 | Phase 6 (1.2) |
| MARBIM comment-sheet extraction not built | The brief wants a drafted feedback round read off a buyer's comment sheet. `sample_feedback_rounds` is a registered pending target with page numbers in its payload and is ready to receive one; there is no extractor. | sampling → marbim | 2026-07-30 | Phase 2 (X.2) |
| **Only AQL 2.5 and 4.0 are seeded** | Migration 0035 seeds ANSI/ASQ Z1.4 level II for the pair essentially every garment buyer specifies. Rows for 1.0, 1.5 and 6.5 must be transcribed from the published standard rather than recalled — one acceptance number out decides whether a container ships. Until then `resolveAqlPlan` refuses that level with a clear error instead of substituting a neighbour. | quality | 2026-07-30 | Before a buyer specifying another level is onboarded |
| Z1.4 arrows resolved to the larger plan | Where the standard's arrow sends 2.5 and 4.0 to different sample sizes (lots of 2–25), both are seeded at the LARGER size. You cannot draw two samples from one carton. Documented in 0035; revisit if a buyer audits the plan. | quality | 2026-07-30 | If a buyer disputes a small-lot plan |
| Only inspection level II is seeded | Levels I and III (reduced/tightened) are in the payload enum but have no rows, so they refuse. | quality | 2026-07-30 | Phase 6 close |
| 4-point per-linear-yard cap not applied | The standard caps a single linear yard at 4 points regardless of how many defects it carries. That needs per-yard defect POSITIONS, which the capture payload does not collect, so the cap is not applied — the score can therefore be harsher than the standard allows. | quality | 2026-07-30 | If a mill disputes a roll rejection |
| `inline_checks.operator_id` has no FK | `employees` is owned by 10.1 Workforce. Per-operator DHU cannot be attributed until it lands. | quality → workforce | 2026-07-30 | Next workforce slice |
| Supplier reject quantities still not fed from QC | 3.2's `qualityRejectPct` needs a rejected QUANTITY per supplier. `fabric_inspections` now records a pass/fail per roll, which is closer, but there is still no rejected-quantity attribution. | quality → procurement | 2026-07-30 | When fabric rejections write back to the GRN |
| Buyer report pack PDF not built | `buyerReportPack()` returns the four sections the pack renders and is tested; the PDF render is document work. | quality | 2026-07-30 | With HANDOFF-7.1 |
| No k6 scenario for inline capture | `inline_checks` is the second-highest-volume table after `hourly_outputs` and is floor-facing, so dev-plan §9 wants `qc_inline.js`. | quality | 2026-07-30 | Phase 4 close |
| `aql_tables` privilege wall depends on the app connecting as `fabricxai_app_rw` | Verified in the integration suite: that role has SELECT and no INSERT. A deployment that connects as the table OWNER would bypass it, since the protection is a grant rather than a policy (`aql_tables` has no `company_id` to write a policy against). | quality | 2026-07-30 | Before first deploy |
| The bank handoff does not reach 2.1 | `handoffDocsToBank` enforces the EXP gate and marks the checklist submitted, then emits `shipment.docs.ready_for_bank`. Module 2.1's own submission record does not exist yet, so nothing on the commercial side receives the set. | shipment → commercial | 2026-07-30 | Next commercial slice |
| Packing list PDF not rendered | `packing_lists.generated` holds the full frozen list and `document_id` is ready; producing the PDF on letterhead is document work. | shipment | 2026-07-30 | With HANDOFF-8.1 |
| Partial shipments share one order-wide balance | `remainingToPack` works order-wide, so a part-shipped order's second shipment draws from the same pool. Correct for the common case; a buyer who allocates specific cells to specific partials is not modelled. | shipment | 2026-07-30 | If a buyer allocates per partial |
| No k6 scenario for carton packing | 8.1 is floor-facing, so dev-plan §9 wants `shipment_pack.js`. | shipment | 2026-07-30 | Phase 4 close |
| `cartons.shipment_id` FK lives in SQL only | `cartons` is declared before `shipments` in one file, so the forward reference is added in migration 0038 rather than in the Drizzle schema. Same reasoning as `btb_lcs.supplier_id`. | shipment | 2026-07-30 | If drizzle gains forward refs |
| The QC waiver is role-gated in code, not routed through pending_changes | `waiveFinalInspection` requires owner or commercial directly, the same shape as costing's below-the-floor rule. A two-approver flow would be stricter; `approvals_required` is not enforced anywhere yet. | shipment | 2026-07-30 | Phase 2 (X.1 Approve Inbox) |
| `doc_submissions.shipment_id` has no FK | `shipments` belongs to 8.1, which already imports commercial for the LC. Declaring it would make the two schemas import each other, same as `btb_lcs.supplier_id`. | commercial | 2026-07-30 | If drizzle gains cycle-free FKs |
| Freight is not in the commercial cost component | The accrual's basis names `excludes: ['freight']` rather than pretending completeness. Freight lives on 3.2's landed cost and on a forwarder invoice, neither attributable to an order yet. | finance → procurement, shipment | 2026-07-30 | When forwarder invoices are modelled |
| Materials accrual prices at the LATEST GRN line for the item | Not at the specific roll's own GRN line, because `issue_lines.roll_id` is nullable (trims are issued by quantity). On a rising market this understates older issues. | finance → store | 2026-07-30 | When issues always carry a priced source line |
| CM allocation is model v1 and needs a stated rate | `loadedLineDayRate` is company policy passed per call. Without it the component is recorded as ZERO with its basis marked unavailable — never omitted, which would flatter the margin. A real payroll allocation belongs with 10.1. | finance → workforce | 2026-07-30 | Phase 8 |
| Tally export not built | The brief asks for CSV/XML export. The data is all in `order_costs_actual`, `receivables` and `payables`; the serialiser is not written. | finance | 2026-07-30 | With HANDOFF-11.1 |
| `receivables.part_realized` is never set | A genuinely partial credit against a part shipment would use it. `postRealizationToReceivable` currently closes or leaves open. | finance | 2026-07-30 | When part-shipment realizations appear |
| No k6 scenario for the cash timeline | It scans every open receivable and payable per call; at factory scale that wants measuring even though the module is not floor-facing. | finance | 2026-07-30 | Phase 4 close |
| Consumers run with `roles: ['owner']` | A system context needs SOME role and there is no `system` role to narrow to; the scheduler already does the same. Company-scoped so RLS binds it as it binds a request, and `userId` is null so nothing is attributed to a person who did not act. Narrowing means auditing every role check in the repo. | core | 2026-07-30 | When a system role is introduced |
| Invoice drafting is not wired to ex-factory | `shipment.ex_factory.confirmed` actualises the TNA milestone but does not draft an 11.1 invoice: the invoice number is a company sequence nobody owns yet, and a consumer inventing one would collide with whatever the finance desk already uses. | shipment → finance | 2026-07-30 | When invoice numbering is settled |
| `planning.sewing_window.changed` still has no consumer | Moving sewing should ripple 1.3's downstream milestones. That is a TNA recompute, not a single-field write, and it belongs behind an orders service operation rather than in the consumer file. | planning → orders | 2026-07-30 | When 1.3 exposes a ripple operation |
| ~~The approve inbox has no HTTP surface~~ **resolved — replaced by a worse gap** | `actions.ts` ships and the 833-LOC inbox client calls all three actions, so X.1 is the most operable desk in the product. It is also **the least verified**: `__tests__/` is empty (887 LOC backend + 833 LOC UI, zero tests) and it has **no `register.ts`**, so it is absent from `modules/registry.ts` — no primer, no pending targets, no zod map. Every ⚖ mutation funnels through it. Batch approve fires `Promise.all` with no per-row failure reporting. | approvals | 2026-07-30 → 2026-08-05 | Before pilot (audit TEST-B1 / BE-B7) |
| Buyer terms are not yet read by the gates that need them | `termsFor(buyerId, onDate)` returns the version in force and is tested, but 7.1 still takes its AQL level from a per-call policy and 8.1 its tolerance from the LC. Wiring them means passing the ORDER's date, not today's. | buyers → quality, shipment | 2026-07-30 | Next slice of each |
| Buyer manual extraction has no extractor | `buyer_requirements` is a registered pending target taking a page-numbered batch, and the commit handler is tested. Nothing produces the draft — that is X.2. | buyers → marbim | 2026-07-30 | Phase 2 (X.2) |
| `buyer_contacts` are not carried on conversion | `convertLead` creates the buyer and links the lead; contacts and activities stay on the lead, reachable through `leads.converted_buyer_id`. Copying them would duplicate a record of conversations that happened once — revisit if the buyer screen needs them inline. | buyers | 2026-07-30 | If the buyer screen needs them |
| Quote PDF and email not built | `quotes.document_id` is ready; rendering the quote on letterhead and sending it is document/notification work. | rfq | 2026-07-30 | With HANDOFF-1.2 |
| RFQ role scoping is not enforced | The brief wants merchandisers to see their own buyers' RFQs and managers to see all. `rfqs.owner_user_id` is set and indexed; no read filters on it, because that belongs at the action boundary which awaits the handoff. | rfq | 2026-07-30 | With HANDOFF-1.2 |
| An order from a won RFQ has NO breakdown | An RFQ carries a size ratio; an order breakdown is a colour × size grid, and the colours come with the buyer's purchase order. The gap is caught by an existing gate — 5.1 refuses to spread a lay against a style with no breakdown — rather than filled with an invented colour. | orders | 2026-07-30 | When the buyer's PO is captured |
| An order from a won RFQ carries a placeholder PO number | `RFQ-<title>` until the buyer's actual purchase order arrives. A real-looking PO number invented here would appear on documents as though a buyer had issued it. | orders | 2026-07-30 | When the buyer's PO is captured |
| Four product types have templates | knit, woven, outerwear, sweater, plus an alias map for what merchandisers actually type. An unrecognised type resolves to null and the order gets NO schedule rather than the shortest one — a jacket on a 90-day calendar has a wrong ship date from day one. | orders | 2026-07-30 | As new product types are quoted |
| Lead times in the defaults are judgement, not measurement | The offsets are plausible for a Bangladeshi factory and are the shape the engine needs; they are not this factory's actual lead times. Tune them in Settings before planning against them. | orders | 2026-07-30 | Before pilot |
| Seeded defect severities are judgement, not a buyer's manual | `severity` is what an AQL verdict is computed against, so a misclassification changes whether lots pass. The set is conservative — injury risk is critical, buyer-rejectable is major, cosmetic is minor — but a QA manager should review it against their own buyers' manuals. | quality | 2026-07-30 | Before pilot |
| Policy is read by the CALLER, not fetched inside services | Deliberate: a module never imports Settings, so services still take their policy as an argument and stay testable without a database. What X.3 changed is that the value now comes from one authoritative row. Every action and job must remember to call `getPolicy` — a call site that passes a literal still compiles. | settings | 2026-07-30 | If a lint rule can catch a literal policy |
| Policy defaults are industry norms, not this factory's numbers | 60% line efficiency, 40 points per 100 sq yd, 75% BTB ceiling, 2% cut tolerance. They exist so a fresh company works; a factory's own history should replace them. | settings | 2026-07-30 | Before pilot |
| ~~`company_profiles.locale` is written by nobody and read by nobody~~ **resolved** | The form has a control, so the value is chosen rather than defaulted — it used to initialise at a hardcoded `'en'` with no way to change it, which meant the first save wrote English with nobody deciding. And it is read: `requestLocale(companyDefault)` places the factory's language above `accept-language` and below an explicit device cookie, wired at the app shell that feeds `LocaleProvider`. **Owed:** the ~24 server components calling `requestLocale()` with no argument still resolve cookie → header → English. | settings → i18n | 2026-07-30 → 2026-08-06 | ✅ (plan 5.8); the direct callers with it |
| Full-company data export job not built | The brief asks for one. `auditTrail` and the module reads exist; assembling and streaming an export is a job with a document sink. | settings | 2026-07-30 | With HANDOFF-X.3 |
| Module toggles are stored but not enforced | `isModuleEnabled` is the read; nothing calls it, so switching a module off records the intent without hiding anything. Enforcement belongs at the action/navigation boundary. | settings | 2026-07-30 | With HANDOFF-X.3 |
| X.3's HTTP surface is four actions wide | Was one. `saveCompanyProfile` is joined by `readAuditTrail`, `grantUserRole` and `revokeUserRole` — so the audit log 10+ modules write is finally readable from a screen, and roles can be assigned without seeding. **Owed:** the policy override editor (`setPolicy` and `saveModulePolicy` both exist; the panel is still read-only), `repairProvisioning`, and the CSV export. Settings also still has no `queries.ts`, so its pages read `service.ts` directly (BE-M6). | settings | 2026-07-30 → 2026-08-06 | Plan 5.8 residue |
| The three SDK call bodies have never run | Plan 6.4 wires Gemini (`extract`), Anthropic (`reason`) and OpenAI (`embed`) behind the seam, with the boot assertion and the logprob→confidence derivation both tested. What is NOT tested is the part that talks to a vendor: no API key exists in this environment, so `client.models.generateContent`, `client.messages.create` and `client.embeddings.create` have never been called. Specifically unproven: that Gemini returns `logprobsResult.chosenCandidates` in the shape zod expects for the configured model, that its `responseJsonSchema` accepts what `z.toJSONSchema` emits for every intake kind, and that the reconstructed token text parses as the JSON the schema wants. All three fail LOUDLY if wrong — the extraction refuses rather than inventing a score — so the risk is a dead feature, not a wrong number. | marbim | 2026-08-06 (plan 6.4) | First run against a real key — one document per intake kind |
| No real model provider is registered | X.2 declares the seam (`MarbimProvider`: extract + generate, by ROLE) and ships the deterministic provider `MARBIM_MOCK` selects. Nothing wires Anthropic/Gemini/OpenAI yet. With no provider registered MARBIM refuses rather than inventing output, so this degrades loudly. | marbim | 2026-07-30 | When keys and budget are agreed |
| Scans are not read — DECIDED, not built (AI-M1) | Intake requires pasted TEXT; an attached PDF is stored for the approver to check against and never read. Plan 6.6 settled the how: **no OCR library, no PDF parser, no Tesseract.** Gemini accepts a PDF or an image as an inline part and reads it directly, which is a consequence of 6.4 routing `extract` there rather than a new dependency — and a separate OCR step would be strictly worse, because it throws away layout before the model sees it (a quantity in a table cell becomes a loose number in a wall of text) and it produces no logprobs, so a scanned draft could carry no measured per-field confidence. **What is owed to build it:** fetch the object from S3 in the worker, cap the bytes, add an inline part beside the fenced text, make `sourceText` optional at the door, and relax the intake screen's text-required rule. The screen already says why it asks for text, so nothing misleads today. | marbim | 2026-08-06 (plan 6.6) | When a factory asks — the pilot pastes from the PDF viewer |
| The deterministic extractor reads LABELLED LINES only | It handles `Quantity: 12,000 pcs` and scores by whether a label named the field and whether the value matched the shape expected for it. It does not read tables, PDFs, or prose. Anything unlabelled is left absent rather than guessed — an earlier shape-anywhere fallback produced `styleCode: "USD"`. | marbim | 2026-07-30 | With a real provider |
| ~~Only 1.6 and 11.2 ship a `toolPack`~~ ~~**MARBIM cannot EXECUTE any tool**~~ **resolved** | All 21 modules now register a versioned `toolPack` and a `domainPrimer`, validated at boot. The real gap moved: **MARBIM cannot EXECUTE any tool.** `chat` passes tool names to the model and stores `toolCalls`; there is no execution loop, and `runDraftTool` — the only path from a tool to `pending_changes` — has zero production callers. ~90 tools are prompt text. The UI no longer pretends otherwise (plan 6.2, 2026-08-06): requested calls render as `requested` — inert slashes, "asked for, not run" — the receipt says "none run", and the footer states plainly that the answer came from the primers and the model's own knowledge. `marbim-claims.test.ts` keeps the claim from coming back. Plan 6.5 landed the execution loop (`loop.ts`), so tools now genuinely run, the strip is genuinely a citation, and the footer is conditional on what happened. `runDraftTool` has its only caller and remains the only door into `pending_changes`. **What is owed now is narrower:** the loop has never run against a live model — the same gap as the SDK bodies above — so the shape of a real multi-tool turn is unproven end to end. | marbim | 2026-07-30 → 2026-08-06 (audit AI-B3) | ✅ `loop.ts`; live verification with the SDK row above |
| Chat-composed drafts carry NO confidence at all | Deliberate, and better than what it replaced: eight modules typed varied per-field constants into their draft tools (`qtyDelta: 0.62` on every stock adjustment forever), which cleared the uniform-value check and drove inbox order, the auto-approve floor and the correction-rate report on a number that never moved. `fieldConfidence` is now off `ToolProposal`, `propose` refuses one on `ai_chat`, and `no-invented-confidence` bans the literal repo-wide (plan 6.3, audit AI-B2). **What that leaves:** an unscored draft cannot be RANKED — every chat draft sits equal in the inbox, and `confidenceMin = null` means it can never auto-approve, so the queue is strictly first-come. That is the honest state until something measures. A real score would be a pass that reads the payload back against the conversation; per-field logprobs off a tool call do not exist. | marbim → core | 2026-08-06 (audit AI-B2) | With 6.4/6.5, if a scoring pass is worth its cost |
| Chat has no retrieval and no embeddings | `chat` assembles the primers, redacts, records the turn with its primer versions, and calls the provider. There is no vector store, so it cannot cite a document — the `embed` model role is declared and unused. | marbim | 2026-07-30 | When retrieval is scoped |
| The embedding is LEXICAL, not semantic | `MARBIM_MOCK`'s embedder hashes tokens into buckets and normalises. Two styles sharing `producttype=tshirt`, `gsm=180` and `construction=single jersey` come out genuinely close and unrelated ones come out at zero — but it does not know "tee" and "t-shirt" are the same garment. Every fingerprint stores the model that made it and `findSimilar` filters on it, so swapping in a real embedder does not silently mix spaces. | memory → marbim | 2026-07-30 | With a real provider |
| Consumption is matched on the store item CODE | A seeded BOM line gets the measured figure when its `item_ref` matches an issued item's code. A line with no item code, or one whose code changed since, keeps its estimate and is marked `planned`. Correct but conservative — it under-reports how much was actually measured. | memory | 2026-07-30 | When BOM lines always carry an item id |
| `mixed_day` — the declared release gate — is not built | Plan 7.1 landed the harness and three scenarios (`production_burst`, `store_grn`, `owner_dashboard`) with committed baselines and automated row assertions. `mixed_day` is deliberately not among them: the dev-plan names it the release gate BECAUSE it is the whole product at once, and thirteen server actions still have no screen behind them (plan 5.5's `NO_SCREEN_YET`). A mixed-load figure over half a product is a number nobody could act on — it would neither pass honestly nor fail informatively. **Also owed regardless:** every baseline in `k6/baselines/` was measured on a developer machine and says so in its own `host.note`; the brief requires VPS-class hardware for the gate, so today they are a regression reference and not a release decision. | all | 2026-08-06 (plan 7.1) | With the 5.5 desks, then re-baselined on the pilot VPS |
| `order_outcomes` has no k6 scenario | The compiler reads six tables and runs on close, one order at a time, off the request path. It is not marked ⚡ and is not floor-facing, so no load scenario was written. Worth one if a factory ever closes a month of orders in a batch. | memory | 2026-07-30 | If batch closing appears |
| The downtime cost rate has no default and nothing computes it | It is now a Settings field (`maintenance.lineValuePerMinute`) and deliberately ships EMPTY: 9.1 refuses to price a stoppage without one, and the monthly job notifies the owner that it could not rather than writing zeroes. Nothing derives it from the gazette wage and the line's SMV — a factory must set it. | maintenance → workforce | 2026-07-30 | When somebody sets it, or 10.1 derives it |
| Utilization takes `availableMinutes` as an argument | 4.1 owns line calendars (shift minus maintenance minus holidays) and 9.1 does not read them yet, so the denominator is supplied by the caller. It REFUSES a zero or exceeded denominator rather than clamping, so a wrong one surfaces instead of reading as 100%. | maintenance → planning | 2026-07-30 | When line calendars are wired |
| Machine `assignment_history` is never read | It is appended correctly on every reassignment and is the record of a machine that keeps being moved — often the reason it keeps breaking — but nothing reports on it yet. | maintenance | 2026-07-30 | With the machine screen |
| `requiredCertificates` per regime ships a short list | RSC needs fire and boiler in the pack; the real per-regime lists are a compliance officer's knowledge and belong in Settings. The pack NAMES what is missing rather than assuming, so a short list under-reports gaps instead of inventing them. | compliance → settings | 2026-07-30 | With the Settings slice |
| No audit-pack export format | `auditPack` returns the contents and the gaps as data. Rendering it as the PDF/zip an auditor is handed is document work, and `lib/pdf.ts` is still a type. | compliance | 2026-07-30 | With the documents slice |
| `caps.milestones` is stored but never checked | Multi-year RSC remediation is a schedule of dated milestones a buyer signed off. The column holds them; nothing warns when one is missed, so only the final deadline escalates. | compliance | 2026-07-30 | Before an RSC cycle |
| No findings extractor exists | `findings_batch_v1` is registered with per-finding source pages and the commit handler is tested through the approve path. Nothing produces the draft from an actual audit report — that needs X.2 with a real provider. | compliance → marbim | 2026-07-30 | With a real provider |
| Two exception kinds have no source | `runrate_miss` and `payroll_anomaly` are in the enum but nothing feeds them. `FEED_COVERAGE` reports this on every read and the refresher never resolves a kind it does not scan, so an empty result for them reads as "nobody looked" rather than "nothing wrong". | analytics → production, workforce | 2026-07-30 | With each source module's slice |
| Aggregations are not cached | The brief wants a 5-minute TTL. `asOf`/`stale` are built and every figure carries them, but nothing is actually memoised in Redis — each call hits Postgres. Correct, just not yet cheap. | analytics | 2026-07-30 | Before pilot load |
| Buyer scorecards have no quality component | `dhu` is passed as null per buyer, so every scorecard comes back unrated on that ground. Attributing DHU to a buyer needs the line-to-order attribution 1.6 already flags as approximate. An absent component is refused rather than scored zero, so this under-rates rather than misleads. | analytics → quality | 2026-07-30 | When per-order DHU exists |
| `saved_reports` and `scheduled_exports` are tables only | Both are declared with RLS and read by 11.2; nothing writes them (rule 9 forbids it here) and no export job renders or sends anything. | analytics | 2026-07-30 | With the worker + documents slices |
| The style embed sweep fingerprints on the style CODE and description only | GSM, construction and gauge are what make similarity meaningful, and they arrive with a tech pack nothing parses yet. The sweep embeds what exists; the fingerprint re-embeds itself when richer attributes land, because the source hash changes. | memory → costing | 2026-07-31 | When tech-pack attributes are captured |
| Email is the only delivery channel | `notifications.channels` still lists `in_app`, `email` and `push`; delivery reads SEVERITY from the `delivery` policy and ignores the column. Nothing sends push, and there are no per-user channel preferences — a user who wants no email cannot say so. | core | 2026-07-31 | With the notifications screen |
| The digest is per company-tenant, not per person across companies | A user who belongs to two factories gets two digests. Correct under RLS — each run is scoped to one company and cannot see the other — and worth revisiting only if multi-company users become common. | core | 2026-07-31 | If multi-company users appear |
| ~~`delivery.appUrl` defaults to localhost~~ **resolved** | The default is now a getter reading `APP_URL`, which is required at boot in production, so the localhost fallback only ever serves unit tests. No Settings surface overrides it yet. | core → settings | 2026-07-31 | ✅ done (audit INFRA-H3) |
| 141 desk refusals are still English-only | Down from 254. **Every floor-reachable refusal is now bilingual**: the floor namespaces (store, cutting, production, quality, sampling, shipment, maintenance) are at parity, and so are all thirty `gates.*` blocks — the refusal that stops work mid-task. What is left is desk copy read at a keyboard by somebody who can escalate it. The set is **enumerated in `i18n.test.ts`, not computed** (plan 4.3), with a four-way ratchet: an unlisted untranslated key fails, a stale entry fails, a phantom entry fails, and no `gates.*` key can be exempted at all. It can only shrink. | core → i18n | 2026-07-31 → 2026-08-06 | Before pilot, or as a Bangla reader goes through them |
| Bangla strings are unreviewed | Written to be plain and correct for a floor reader, keeping the terms a Bangladeshi factory uses in English anyway (LC, UD, PM, ex-factory). No native speaker has read them. | core | 2026-07-31 | Before pilot |
| Job health is per company, so N tenants means N alerts | A dead scheduler notifies every company's owner separately. Correct under RLS — each run is scoped and cannot see the others — but on a large fleet one outage is a lot of email. `/api/health/jobs` is the cross-tenant view and is where an operator should watch from (plan 7.5). | core | 2026-07-31 | If the fleet grows |
| `/api/health/jobs` reports the scheduler across ALL companies | One tenant being quiet cannot turn the deployment red, so the endpoint uses a looser budget (3× interval, 30-minute floor) and asks only whether each task succeeded somewhere recently. It answers "is the worker running", not "is every tenant being served". Moved off `/api/health` and behind `HEALTH_TOKEN` by plan 7.5 — naming every scheduled task and what is broken in it is reconnaissance, not a status page. | core | 2026-07-31 → 2026-08-07 | — by design |
| A stuck run is detected but never cleared | `stuckRuns` reports a `running` row older than the threshold and the health alert names it. Nothing marks it failed or re-queues it — that needs deciding whether a killed job should be retried, which depends on the task. | core | 2026-07-31 | When a job actually gets stuck |
| The cron classifier knows four shapes | Sub-hourly steps, hourly, daily and monthly — every pattern this system schedules, asserted by a test over `SCHEDULED_TASKS`. A weekly or business-hours pattern is REFUSED rather than monitored at a default, so adding one means teaching the classifier. | core | 2026-07-31 | When a new shape is scheduled |
| Monthly tasks are slow to notice | At 1.5× a 31-day interval, a monthly job that stops firing is reported after about 46 days. Inherent to a monthly cadence; the alternative is false alarms on 31-day months. | core | 2026-07-31 | — by design |
| ~~The order workspace has no Costing tab~~ **resolved differently** | Costing reaches the workspace not as a tab but as a PANEL on Style & documents, which is where a merchandiser looks for the cloth. `costing.styleBom` is the order-scoped read that was missing: it returns null instead of throwing when a style has no approved sheet, prefers the approved sheet's BOM, and says which of the two it gave you. The priced sheet itself is still costing's own screen — the dossier shows consumption, not margin. | orders → costing | 2026-08-22 → 2026-08-27 | ✅ done (design canvas, Style & documents) |
| The pulse knows five facts, not every blocker | `orderPulse` reads late/at-risk milestones, sampling's PP gate, and the shipment board's EXP and LC-latest-shipment numbers. The other structured gates — BTB headroom, UD balance, final and fabric inspection — are not on the strip: each belongs to a module whose order-scoped read either does not exist or would need the pulse to reach across a boundary the caller must assemble instead. Open requests join the same shape when X-4 lands, which is why a fact is `{key, params, severity}` rather than a sentence. | orders → commercial, store, quality, requests | 2026-08-22 | As each module exposes an order-scoped gate read (and with X-4) |
| The dev database's seed identities predate tenant-scoped emails | The seeded people for the seed company exist as `store@seed-apparels.test`, while `seedEmail` now computes `store+00000000@seed-apparels.test`; the seed inserts by user id with `onConflictDoNothing`, so a re-seed leaves the old addresses in place. Every e2e sign-in therefore fails locally with "that email and password did not match", and `pnpm test:e2e` cannot run against this database until those rows are dropped and re-seeded. Environment state, not product code — recorded because it silently disables the e2e suite. | e2e / dev seed | 2026-08-22 | Before the next e2e run — drop the stale seed users and re-seed |
| The drawer framework has one adoption site and one shape | Core PR 2 (specs/order-centric-core.md §3) shipped the mechanism whole — registration seam, activation + role walls, refs-driven resolution, the one-level stack — proved through two kinds (order, document) and one screen (the shipment board's PO chip). Owed: EntityRef adoption across the screens that print codes today, and the other two spec §3 shapes — draft review (the approve screens converging on one component) and compose (the X-4 request compose). The registration is an object so those extend it without reshaping modules. | core → all screens | 2026-08-22 | With the requests module (compose) and approve-screen convergence (draft review) |
| Department actions do not call `assertModuleActive` at their own boundaries yet | Core PR 1 (specs/order-centric-core.md §1) shipped the mechanism; three choke points now consume it — the MARBIM surface (chat scope, primers, packs, every intake door), the Settings screen (`setCompanyModule` flips modules with dependency hints from the same graph the service refuses by), and the shell (`visibleNav` subtracts the inactive set, `resolveAccess` refuses a switched-off module's URL for everyone with the honest "switched off for this factory" card, `landingFor` never lands anybody on one). What remains is the fourth: no department ACTION calls `assertModuleActive` beside its auth and zod, so a client that skips the shell — a direct action invocation, the offline batch endpoint — still reaches a switched-off module's services. The shell wall covers the product as shipped; the action wall is what makes the gate hold against any caller. | core → all | 2026-08-22 | The next slice of the order-centric sequence |
| ~~The dossier shows no department sign-off panel~~ **built, five of seven** | The panel ships with the five gates that are readable today — the quote (rfq, matched on style code, since an enquiry becomes an order through a person and not a foreign key), the cost sheet (costing, DRAFT included: an order made against a price nobody signed is the state worth seeing), the credit and BTB headroom (commercial), the supplier POs (procurement, through the indexed `purchase_requisitions.order_id`), and the PP verdict (sampling, with the round and whether it was "approved with comments", which the gate cannot distinguish and the floor must). **The two that do not exist still do not**, and the objection this row raised is answered by giving them their own state rather than omitting them: `unmodelled` renders "not recorded here" in words, so five-of-seven cannot read as "the other two are not done". The order confirmation sheet is not really a missing document but a missing SEQUENCE — `pending_change_approvals` already stores who signed, as what role and when; the engine just does not require the fourth signature to follow the third. The sales contract is missing only its IDENTITY: its substance (tolerance from the order, AQL and the nominated lab from the buyer's terms as at the order's plan date) is already in the database and is now printed beneath the row. **Owed:** the contract's number, date and file — two nullable columns and a document link — and the signature chain, both of which need a real example of each paper from a factory. Nothing in the Nordkap kit's 28 documents is either one. | orders → rfq, costing, commercial, procurement, sampling | 2026-08-27 | ✅ panel 2026-08-29 · the two artefacts when a factory supplies one of each |
| The style dossier's identity fields have one writer and no reader in MARBIM | `season`, `customerLabel`, `patternNo`, `basedOnStyle` and `packingMethod` are written by `updateStyleDetails` (the inline form) and by `createOrder` from the PO draft — `orderStyleDraft` carries them, so an extraction can fill them. Nothing yet READS a tech pack for them: the fields the buyer states most reliably arrive on a tech pack, and `costing`'s tech-pack extractor writes a BOM, not a style identity. Until then most orders show "not recorded" until somebody types it. | orders → marbim, costing | 2026-08-27 | When the tech-pack extractor writes style identity |
| The action-reachability ratchet can be fooled by a service sharing its action's name | The scan asks whether any file outside an `actions.ts` mentions the action's identifier. A service function with the SAME name as its action satisfies that from its own definition, so a genuinely unreachable action passes. Found while wiring `updateStyleDetails`, whose screen now exists — so nothing is currently hidden by this — but the hole is real and the repo convention that avoids it (`import { x as xIn }`, service named differently) is convention, not enforcement. Fixing it means the scan must exclude each module's own `service.ts` definition line, or actions must be found by call shape rather than by name. | core → tests | 2026-08-27 | Before the ratchet is trusted for a module nobody reviewed |
| ~~The canvas's "Verify & apply" would be self-approval on a ⚖ table~~ **resolved — the design was right** | The question this row posed — is a human reading an AI's draft the FIRST reviewer (design) or the second (platform)? — was answered the design's way on 2026-08-29, and the reasoning is worth keeping: when a merchandiser pastes the buyer's amendment mail, there ARE two parties. The extractor read the mail and proposed a colour × size grid; the merchandiser compared that grid to the mail and disposed. The trail records the model, its per-field confidence and every correction the person made against it, which is a fuller review record than most second signatures carry. Sending it to an approver who does not have the mail was the weaker control, not the stronger one. So the button is built, and bounded three ways: the module must declare it (`orders` does; `commercial` does not, so an LC read off a SWIFT advice still needs two names), the factory may switch it off for a buyer audit, and the source must be a document extraction no matter what either says. | orders → core | 2026-08-27 | ✅ 2026-08-29 |
| The amendment extraction must carry forward cells the document does not repeat | `order_revision_v1` takes the WHOLE grid because `applyRevision` replaces the revision wholesale — a partial update leaves cells the new grid does not mention, and "the ratio still has an XXL nobody ordered" is a real way to over-cut. A buyer's amendment mail states only the changed cells, so the extractor has to read the four it names and carry over the eleven it does not, from the order's current revision — which it cannot see. The kit fixture records this as the finding to watch for (four cells sum to 15,600, not 42,000) and the tolerance check refuses the draft at approve, so it fails loudly. What is missing is giving the extractor the current grid as context so it can do the arithmetic at all. | orders → marbim | 2026-08-27 | With the next extractor-context slice |
