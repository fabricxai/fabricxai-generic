# deps
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — secrets only via env, never in the image (dev-plan §6)
.env
.env.*
!.env.example
!.env.production.example
!.env.backup.example

# Deploy-time secrets written on the host: the pooler's one-line userlist, and anything
# else an operator generates. Never committed, never baked into an image.
/secrets/

# local infra state
.docker-data/
docker/data/

# test / build output
coverage/*
!coverage/floor.json
dist/
playwright-report/
test-results/

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# k6 run artefacts: live session tokens and the last summary. Never committed.
k6/.sessions.json
k6/.last-summary.json

# Playwright saved sessions and run output.
e2e/.auth/
test-results/
playwright-report/
e2e-results.xml

# Test reports (JUnit). Uploaded by CI, never committed.
reports/
