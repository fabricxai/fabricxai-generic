# FabricXAI — single-factory production deployment.
#
# One VPS, one factory, one database. That is the shape the product is for, and this file
# is deliberately the smallest thing that is defensible rather than a Kubernetes on-ramp:
# an ERP that a Bangladeshi factory's own IT person has to keep running at 2am should be
# readable in one sitting.
#
#   caddy      TLS termination and the only published ports (80/443)
#   app        Next.js — the HTTP surface
#   worker     BullMQ — schedules, event consumers, notifications
#   migrate    one-shot; runs migrations and provisions roles, then exits
#   postgres   the database — built locally, because it must carry pgbackrest
#   pgbouncer  transaction pooling, scram auth, no password on disk
#   redis      queues; AOF on, noeviction, password-protected
#   minio      object storage for documents
#   backup     pgBackRest, on demand (profile: backup) — never runs as a daemon
#   docs-sync  rclone MinIO→offsite, every 15 minutes (profile: backup)
#
# Bring up:      docker compose -f docker-compose.prod.yml --env-file .env.production up -d
# Deploy a build: see docs/runbooks/deploy.md — migrate runs BEFORE app/worker start.
#
# BACKUPS ARE PART OF THIS FILE NOW (audit INFRA-B3). `.env.backup` supplies the
# repository credentials to `postgres` (for archive_command), `backup` and `docs-sync`;
# it is optional at compose level only so a scratch host can come up without it. A
# deployment holding factory data with no `.env.backup` archives no WAL — postgres logs
# the archive_command failure every 5 minutes, loudly, which is the intended behaviour.
#
# Restores stay REHEARSED, not assumed: docs/runbooks/RESTORE-RUNBOOK.md is the drill,
# scripts/restore-verify.sh runs a real weekly restore, docs/runbooks/restore.md is the
# incident procedure.

name: fabricxai

x-app-env: &app-env
  NODE_ENV: production
  APP_URL: ${APP_URL:?set APP_URL to the https:// address the factory uses}
  BETTER_AUTH_URL: ${APP_URL}
  BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?generate with openssl rand -base64 32}
  # Through the pooler, as the non-owner app role. This is the connection RLS binds.
  DATABASE_URL: postgres://${APP_DB_USER:-fabricxai_app_rw}:${APP_DB_PASSWORD:?}@pgbouncer:6432/${POSTGRES_DB:-fabricxai}
  # Direct, as the owner. Migrations only — the app asserts at boot that this role is
  # NOT the one it serves requests with.
  DIRECT_DATABASE_URL: postgres://${POSTGRES_USER:-fabricxai}:${POSTGRES_PASSWORD:?}@postgres:5432/${POSTGRES_DB:-fabricxai}
  REDIS_URL: redis://default:${REDIS_PASSWORD:?}@redis:6379
  # Server-side endpoint, reachable only on the compose network.
  S3_ENDPOINT: http://minio:9000
  # Browser-facing endpoint for presigned upload/download URLs. WITHOUT THIS, every
  # upload from a floor tablet fails: SigV4 signs the Host header, so a URL signed for
  # `minio:9000` cannot be rewritten client-side and the tablet cannot resolve it
  # anyway (audit INFRA-H1).
  #
  # The bare origin, with NO path prefix. It was `${APP_URL}/s3`, which made every presigned
  # URL sign a path that Caddy then rewrote before MinIO saw it — 403 SignatureDoesNotMatch
  # on every upload and download. SigV4 signs the path; the path the client signs must be the
  # path MinIO serves. Caddy routes `/{S3_BUCKET}/*` to minio (see docker/caddy/Caddyfile).
  S3_PUBLIC_ENDPOINT: ${APP_URL}
  S3_BUCKET: ${S3_BUCKET:-fabricxai}
  S3_REGION: ${S3_REGION:-us-east-1}
  S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:?}
  S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?}
  S3_FORCE_PATH_STYLE: 'true'
  # Mail: EITHER an SMTP host OR a Resend key — `lib/env.ts` requires at least one in
  # production and names both in the failure. Neither is `:?` here, deliberately: this file
  # used to demand SMTP_HOST, which meant a correct Resend-only deployment could not start
  # and compose blamed a missing SMTP server on a deployment that does not use one. The
  # check belongs in one place, and env.ts is the place that knows the rule.
  SMTP_HOST: ${SMTP_HOST:-}
  SMTP_PORT: ${SMTP_PORT:-587}
  EMAIL_FROM: ${EMAIL_FROM:?the address mail is sent FROM — must be on a domain the provider has verified}
  RESEND_API_KEY: ${RESEND_API_KEY:-}
  # Optional. Set it and errors are aggregated and alertable; leave it and the app warns
  # at startup and logs to stdout only. Logs are structured JSON either way.
  SENTRY_DSN: ${SENTRY_DSN:-}
  # Bearer token for /api/health/jobs — the route the worker's own healthcheck is disabled
  # in favour of (see the worker service below). WITHOUT IT the route refuses with a 503
  # and says so, deliberately: it names every scheduled task and what is currently broken,
  # which is reconnaissance rather than a status page. Generate with `openssl rand -hex 24`.
  HEALTH_TOKEN: ${HEALTH_TOKEN:-}
  WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-4}
  # Stands the auth rate limiter down, sign-in included — for a tenant holding TEST data
  # being walked through by several people from one address. Empty means the limiter runs,
  # which is what production wants; only an exact `1` disables it.
  #
  # Passed through here on purpose. Setting it in .env.production alone would have done
  # nothing at all — the same trap MARBIM_MODEL_EXTRACT fell into below, and the deploy
  # contract test now refuses any documented variable that no consumer reads.
  RATE_LIMIT_DISABLED: ${RATE_LIMIT_DISABLED:-}
  # Off until a real provider is registered (audit AI-B1). With it on, env.ts requires
  # at least one of the three keys below — not all three, as it used to.
  # Enabling this requires ANTHROPIC_API_KEY specifically, not just any of the three: the
  # assistant button and /marbim do exactly one thing, ask the reason model, so a
  # Gemini-only deployment ships a copilot that renders perfectly and fails on every
  # question (plan 6.4).
  MARBIM_ENABLED: ${MARBIM_ENABLED:-false}
  # Models by role. Absent from this file until now, which meant `MARBIM_MODEL_EXTRACT` in
  # .env.production was read by NOTHING: compose never passed it, so `env.ts` fell through to
  # its own default and the operator's choice was silently discarded. It surfaced the first
  # time somebody needed to move document extraction off Gemini — the variable was set, the
  # container had never heard of it, and the only symptom was extraction still failing
  # against the vendor it was supposed to have stopped using.
  #
  # Empty is safe and means "use the default": `env.ts` strips blanks before parsing, so an
  # unset variable here and an absent one are the same thing.
  MARBIM_MODEL_EXTRACT: ${MARBIM_MODEL_EXTRACT:-}
  MARBIM_MODEL_REASON: ${MARBIM_MODEL_REASON:-}
  MARBIM_MODEL_EMBED: ${MARBIM_MODEL_EMBED:-}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  GEMINI_API_KEY: ${GEMINI_API_KEY:-}
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}

# Every long-running service gets these. Logs are capped because a factory VPS with a
# full disk is a factory with no ERP, and the default json-file driver grows forever.
x-service: &service
  restart: unless-stopped
  logging:
    driver: json-file
    options:
      max-size: '10m'
      max-file: '5'

services:
  caddy:
    <<: *service
    image: caddy:2.8-alpine
    container_name: fxai-caddy
    # The ONLY published ports in this file. Postgres, Redis and MinIO are reachable
    # only on the compose network — a database published to 0.0.0.0 on a VPS is found
    # by a scanner within hours.
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./docker/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    environment:
      APP_DOMAIN: ${APP_DOMAIN:?the bare hostname, e.g. erp.example.com — Caddy gets its certificate for this}
      TLS_EMAIL: ${TLS_EMAIL:?an address Let's Encrypt can reach about expiry}
      # The object-storage route matches this exact path. It must be the same bucket the app
      # signs URLs for, or uploads 404 against the application instead of reaching MinIO.
      S3_BUCKET: ${S3_BUCKET:-fabricxai}
    depends_on:
      app:
        condition: service_healthy

  app:
    <<: *service
    image: ${IMAGE:?the image reference to deploy, e.g. ghcr.io/org/fabricxai@sha256:…}
    container_name: fxai-app
    environment: *app-env
    depends_on:
      migrate:
        condition: service_completed_successfully
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    # Read-only root filesystem: nothing in the app writes to disk. Next needs a
    # writable cache dir, so that one path is a tmpfs rather than the whole tree.
    read_only: true
    tmpfs:
      - /tmp
      - /app/.next/cache
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    # LIVENESS, and only that (plan 7.5). `/api/health` answers 200 because the request
    # arrived; it touches no dependency, so a database blip can no longer make Docker
    # restart a process that is serving requests perfectly well. Readiness is Caddy's
    # question — see the active health check on the upstream — because taking an instance
    # out of ROTATION and restarting it are different remedies for different faults.
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 1200M

  worker:
    <<: *service
    image: ${IMAGE:?}
    container_name: fxai-worker
    command: ['tsx', 'src/worker/index.ts']
    environment: *app-env
    depends_on:
      migrate:
        condition: service_completed_successfully
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy
    # The worker serves no HTTP, so it must NOT inherit the image's HEALTHCHECK — it
    # would be permanently unhealthy and either flap restarts or teach the operator to
    # ignore health status entirely (audit INFRA-H4). Its liveness is observable
    # instead through /api/health/jobs on the app, which reports a schedule that has
    # stopped firing.
    healthcheck:
      disable: true
    # Long enough for the graceful drain in worker/index.ts to finish a job in flight.
    stop_grace_period: 40s
    read_only: true
    tmpfs:
      - /tmp
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    deploy:
      resources:
        limits:
          memory: 900M

  # One-shot. Runs to completion before app and worker start, which is the whole point:
  # nothing at boot stops a new image serving requests against an un-migrated schema, so
  # the ordering is enforced here rather than remembered (audit INFRA-H5).
  #
  # Safe to re-run — migrations are forward-only and the role setup is idempotent, so
  # this is also the rotation procedure for the app and pooler passwords.
  migrate:
    image: ${IMAGE:?}
    container_name: fxai-migrate
    restart: 'no'
    command: ['sh', '-c', 'tsx src/db/migrate.ts && node scripts/setup-db-roles.mjs']
    environment:
      <<: *app-env
      # Only this service needs it: it is what writes the pooler's lookup password.
      PGBOUNCER_AUTH_PASSWORD: ${PGBOUNCER_AUTH_PASSWORD:?generate with openssl rand -base64 24}
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    <<: *service
    # BUILT, not pulled — the image must carry pgbackrest, because archive_command runs
    # inside this container. docker/postgres/Dockerfile explains why no sidecar can do
    # it. First bring-up and every base-image bump needs:
    #   docker compose -f docker-compose.prod.yml --env-file .env.production build postgres
    build:
      context: .
      dockerfile: docker/postgres/Dockerfile
    image: ${POSTGRES_IMAGE:-fabricxai/postgres-pgbackrest:pg16}
    container_name: fxai-postgres
    # Repository credentials for archive_command. Optional at compose level so a scratch
    # host comes up without it; a production host without it archives nothing.
    env_file:
      - path: .env.backup
        required: false
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-fabricxai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?}
      POSTGRES_DB: ${POSTGRES_DB:-fabricxai}
      # scram for client auth, and initdb writes it into pg_hba.
      POSTGRES_INITDB_ARGS: '--auth-host=scram-sha-256 --auth-local=scram-sha-256'
    command:
      - postgres
      # WAL settings a backup tool needs. Set here rather than left to pgBackRest's
      # installer so the database is ready for archiving the day it is configured, and
      # so turning backups on is not also a restart of the database.
      - -c
      - wal_level=replica
      - -c
      - archive_mode=on
      # THE line that makes RPO 15min real. It used to be `true` — a command that
      # succeeds without archiving anything, which meant WAL was generated, recycled and
      # never shipped, and `archive_mode=on` above was decoration. Every backup taken in
      # that state could only be restored to the instant the backup ended.
      #
      # Overridable so a scratch or staging host can go back to `true` deliberately,
      # rather than by forgetting.
      - -c
      - archive_command=${POSTGRES_ARCHIVE_COMMAND:-pgbackrest --stanza=fabricxai archive-push %p}
      # A WAL segment is archived when it FILLS or when this timer expires. Without it,
      # a quiet afternoon on the order desk leaves the current segment unarchived
      # indefinitely and the real recovery point drifts hours past the promise. 5 minutes
      # is a third of the RPO budget; the cost is one mostly-empty segment per idle
      # period, which zstd compresses to a few kilobytes.
      - -c
      - archive_timeout=${POSTGRES_ARCHIVE_TIMEOUT:-300}
      - -c
      - max_wal_senders=3
      - -c
      - shared_preload_libraries=pg_stat_statements
      # 200 is headroom, not a working number. PgBouncer runs transaction pooling with
      # `default_pool_size=25` + a reserve of 5, so real backends top out around 30; the
      # rest of the slots exist so a migration or a psql session is never refused.
      - -c
      - max_connections=200

      # ── Tuning, sized for THIS container on a small VPS ────────────────────
      #
      # Postgres ships `shared_buffers=128MB`, which inside a 2G container leaves most of
      # the limit unused, and assumes `effective_cache_size=4GB` it cannot verify. On a
      # 64GB host nobody notices. On a 12GB VPS — 5.5GB of container limits, ~1.25GB of
      # host and daemon, so roughly 5GB of page cache left — it is the difference between
      # the owner dashboard feeling instant and feeling slow.
      #
      # Every value is overridable, because the right number depends on the box and the
      # next factory's may not be this one.
      #
      #   shared_buffers        25% of the container limit, the usual starting ratio.
      #   effective_cache_size  NOT an allocation — a hint about what the OS is likely
      #                         holding. Set below the real free page cache so the planner
      #                         does not assume more than the host can give it.
      #   work_mem              PER SORT, per backend. 8MB × ~30 real backends is ~240MB
      #                         worst case, which fits beside shared_buffers inside 2G.
      #                         Raise it only alongside the container limit.
      #   maintenance_work_mem  VACUUM and CREATE INDEX. Migrations build indexes; this is
      #                         the one value where being generous costs nothing at rest.
      - -c
      - shared_buffers=${POSTGRES_SHARED_BUFFERS:-512MB}
      - -c
      - effective_cache_size=${POSTGRES_EFFECTIVE_CACHE_SIZE:-3GB}
      - -c
      - work_mem=${POSTGRES_WORK_MEM:-8MB}
      - -c
      - maintenance_work_mem=${POSTGRES_MAINTENANCE_WORK_MEM:-256MB}

      # ── NVMe ───────────────────────────────────────────────────────────────
      #
      # The highest-value two lines here. `random_page_cost=4` is Postgres's default and
      # it describes a spinning disk: it tells the planner a random read costs four times
      # a sequential one, so on anything but tiny tables it prefers a sequential scan to
      # an index. On NVMe the ratio is nearly 1, and this deployment added 24 indexes for
      # cascade FKs (audit DB-M4) that the default would talk the planner out of using.
      #
      # If a future host is NOT on NVMe, raise these back — they are a claim about the
      # hardware, and a wrong claim here produces bad plans rather than an error.
      - -c
      - random_page_cost=${POSTGRES_RANDOM_PAGE_COST:-1.1}
      - -c
      - effective_io_concurrency=${POSTGRES_EFFECTIVE_IO_CONCURRENCY:-200}

      # ── WAL ────────────────────────────────────────────────────────────────
      #
      # pgBackRest streams this offsite continuously, so WAL volume is bandwidth and
      # storage somebody pays for. Compression trades a little CPU — of which this box has
      # spare — for less of both. Spreading the checkpoint over 90% of the interval keeps
      # a floor write from landing during an I/O spike.
      - -c
      - wal_compression=on
      - -c
      - checkpoint_completion_target=0.9
      - -c
      - max_wal_size=${POSTGRES_MAX_WAL_SIZE:-2GB}
      - -c
      - min_wal_size=512MB
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
      # Shared with the `backup` service. pgBackRest reaches Postgres over the unix
      # socket — it has no TCP option for that connection — so the socket directory has
      # to be a volume both containers see.
      - pgsocket:/var/run/postgresql
      - ./docker/backup/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
      # Async archiving spools here before shipping. On a volume, not a tmpfs: a restart
      # with a full spool must not discard WAL that has not reached the repository yet.
      - pgbackrest-spool:/var/spool/pgbackrest
      - pgbackrest-log:/var/log/pgbackrest
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-fabricxai} -d ${POSTGRES_DB:-fabricxai}']
      interval: 10s
      timeout: 5s
      retries: 12
    deploy:
      resources:
        limits:
          memory: 2G

  pgbouncer:
    <<: *service
    image: edoburu/pgbouncer:v1.24.1-p1
    container_name: fxai-pgbouncer
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      # The production config: scram + auth_query, no application password on disk.
      - ./docker/pgbouncer/pgbouncer.prod.ini:/etc/pgbouncer/pgbouncer.ini:ro
      # ONE line, for pgbouncer_auth only, written at deploy time and never committed.
      # docs/runbooks/deploy.md generates it. NOT the dev userlist.txt.
      - ${PGBOUNCER_USERLIST:-./secrets/pgbouncer-userlist.txt}:/etc/pgbouncer/userlist.txt:ro
    healthcheck:
      test:
        - CMD-SHELL
        - 'pg_isready -h 127.0.0.1 -p 6432 -U ${APP_DB_USER:-fabricxai_app_rw} -d ${POSTGRES_DB:-fabricxai}'
      interval: 10s
      timeout: 5s
      retries: 12

  redis:
    <<: *service
    image: redis:7-alpine
    container_name: fxai-redis
    # AOF on and noeviction are not tuning, they are correctness for BullMQ: an evicted
    # key is a job that silently never runs, and there is no record that it existed.
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD:?}
      - --appendonly
      - 'yes'
      - --appendfsync
      - everysec
      - --maxmemory-policy
      - noeviction
    volumes:
      - redisdata:/data
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli -a "$$REDIS_PASSWORD" ping | grep -q PONG']
      interval: 10s
      timeout: 5s
      retries: 10
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?}
    deploy:
      resources:
        limits:
          memory: 700M

  minio:
    <<: *service
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: fxai-minio
    command: ['server', '/data', '--console-address', ':9001']
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?}
      # Browser-facing base for presigned URLs, matching S3_PUBLIC_ENDPOINT.
      MINIO_BROWSER_REDIRECT_URL: ${APP_URL}/s3
    volumes:
      - miniodata:/data
    healthcheck:
      test: ['CMD-SHELL', 'mc ready local || exit 1']
      interval: 15s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 700M

  # One-shot bucket setup: private ACL and versioning. Versioning matters more than it
  # looks — a re-uploaded challan photo overwriting the one a customs officer already
  # saw is not recoverable without it.
  minio-init:
    image: minio/mc:RELEASE.2025-04-16T18-13-26Z
    container_name: fxai-minio-init
    restart: 'no'
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - sh
      - -c
      - |
        set -e
        mc alias set local http://minio:9000 "$$MINIO_ROOT_USER" "$$MINIO_ROOT_PASSWORD"
        mc mb --ignore-existing "local/$$S3_BUCKET"
        mc anonymous set none "local/$$S3_BUCKET"
        mc version enable "local/$$S3_BUCKET"
        echo "[minio-init] $$S3_BUCKET is private and versioned"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?}
      S3_BUCKET: ${S3_BUCKET:-fabricxai}

  # ── Backups ──────────────────────────────────────────────────────────────────────
  #
  # Both are `profiles: [backup]`, so `up -d` never starts them and nothing here runs as
  # a daemon. They are invoked by scripts/ and by the systemd timers in ops/systemd/;
  # `docker compose run` activates a service's profile automatically.

  # pgBackRest, from the same image as postgres so the binary and the server agree on
  # version. Runs as `postgres` because that is who owns the data directory it restores
  # into and the spool it writes.
  backup:
    build:
      context: .
      dockerfile: docker/postgres/Dockerfile
    image: ${POSTGRES_IMAGE:-fabricxai/postgres-pgbackrest:pg16}
    container_name: fxai-backup
    profiles: [backup]
    restart: 'no'
    entrypoint: ['pgbackrest']
    user: postgres
    env_file:
      - path: .env.backup
        required: false
    environment:
      # Tracks .env.production rather than being a second place to change the same name.
      PGBACKREST_PG1_USER: ${POSTGRES_USER:-fabricxai}
      PGBACKREST_PG1_DATABASE: ${POSTGRES_DB:-fabricxai}
      # `--auth-local=scram-sha-256` means even the unix socket wants a password, and
      # libpq reads it from here. archive-push does NOT need this (it copies a file and
      # never connects); backup, check and stanza-create do.
      PGPASSWORD: ${POSTGRES_PASSWORD:?}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - pgsocket:/var/run/postgresql
      - ./docker/backup/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
      - pgbackrest-spool:/var/spool/pgbackrest
      - pgbackrest-log:/var/log/pgbackrest

  # rclone, MinIO → offsite object storage, every 15 minutes (ops/systemd).
  #
  # This replaced a nightly `mc mirror`. The database's RPO was 15 minutes and the
  # documents' was 24 hours, which means a restore could produce a GRN row whose challan
  # photo did not exist — the row says a customs officer was shown a document and the
  # bytes are gone. One RPO, both halves.
  docs-sync:
    image: rclone/rclone:1.71
    container_name: fxai-docs-sync
    profiles: [backup]
    restart: 'no'
    env_file:
      - path: .env.backup
        required: false
    environment:
      # rclone builds remotes straight from the environment — no config file on disk,
      # no credentials in a mounted volume. `src` is this deployment's MinIO.
      RCLONE_CONFIG_SRC_TYPE: s3
      RCLONE_CONFIG_SRC_PROVIDER: Minio
      RCLONE_CONFIG_SRC_ENDPOINT: http://minio:9000
      RCLONE_CONFIG_SRC_ACCESS_KEY_ID: ${MINIO_ROOT_USER:?}
      RCLONE_CONFIG_SRC_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?}
      RCLONE_CONFIG_SRC_FORCE_PATH_STYLE: 'true'
      # `dst` comes entirely from .env.backup (RCLONE_CONFIG_DST_*), so the offsite
      # provider can be changed without touching this file.
      S3_BUCKET: ${S3_BUCKET:-fabricxai}

volumes:
  pgdata:
  redisdata:
  miniodata:
  caddy-data:
  caddy-config:
  # Shared between postgres and backup — see the postgres service.
  pgsocket:
  pgbackrest-spool:
  pgbackrest-log:
